name: Build UptimeWidget

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  build_UptimeWidget:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: dotnet version
        run: dotnet --version
       
      - name: wix version
        run: wix --version